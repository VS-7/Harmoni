import React from 'react';
import { AppLayout } from './app/layout/AppLayout.tsx';

/**
 * Raiz do app: a interface no estilo Spotify. O shell Liquid Glass anterior continua em
 * app/shell e shared/ui, preservado sem uso.
 */
export const App: React.FC = () => <AppLayout />;
